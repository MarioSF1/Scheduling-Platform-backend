services:
  postgres:
    image: postgres:16-alpine
    container_name: scheduling-platform-db
    environment:
      POSTGRES_DB: scheduling_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: